# Generate VAPID keys with:
# npx web-push generate-vapid-keys --json
# Local CORS origin for `wrangler dev`; overrides wrangler.toml vars in local dev.
APP_ORIGIN=http://localhost:5173
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:your-email@example.com
# Completed requests older than this (days) are purged by daily cron. Set 0 to disable.
COMPLETED_RETENTION_DAYS=30
# Daily maintenance guardrails (free-tier protection).
MAINTENANCE_MAX_DELETE_PER_RUN=2000
MAINTENANCE_MAX_BATCHES_PER_RUN=20
