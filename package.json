{
  "name": "renrakun",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev:web": "pnpm --filter @renrakun/web dev",
    "dev:api": "pnpm --filter @renrakun/api dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck"
  },
  "pnpm": {
    "overrides": {
      "minimatch": "^10.2.1"
    }
  }
}
